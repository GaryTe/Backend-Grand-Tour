export enum EnvValidationMessageForApplication {
  AppHost = 'Specify the environment variable HOST in the file .env.',
  AppPort = 'Specify the environment variable PORT in the file .env. The min value port 1000, max value port 6000',
}
